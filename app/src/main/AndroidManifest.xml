<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        PRIVACY GUARANTEE: No network permissions are declared.
        This app cannot send any data anywhere — enforced at the OS level.
        Permissions intentionally absent:
          INTERNET, ACCESS_NETWORK_STATE, READ_CONTACTS, READ_CALL_LOG,
          READ_EXTERNAL_STORAGE, RECEIVE_BOOT_COMPLETED
    -->

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".BigSquareKeysApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BigSquareKeys">

        <!-- Main launcher: Settings / onboarding entry point -->
        <activity
            android:name=".settings.SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding shown on first launch -->
        <activity
            android:name=".onboarding.OnboardingActivity"
            android:exported="false"
            android:label="@string/onboarding_title" />

        <!-- The IME service — this is what replaces Samsung/Gboard -->
        <service
            android:name=".ime.BigSquareKeyboardService"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>

</manifest>
